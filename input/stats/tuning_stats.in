! $Id$
! This file is used by the script run_tuner.bash.  It contains a list of the 
! variables being tuned for while running the tuner and should match
! the CLUBB "t_variables" listed in error_<CASE_NAME>.in.
$statsnl

vars_zt =
'cloud_frac', 'rcm',
''

vars_zm =
''

vars_sfc =
''
/
